# REPLACE_ME

**One-line description.**

## Install

```bash
curl -fsSL https://raw.githubusercontent.com/harshm121/volt/main/packs/REPLACE_ME/install.sh | bash
```

Or, from a clone:

```bash
bash packs/REPLACE_ME/install.sh
```

## Uninstall

```bash
bash packs/REPLACE_ME/install.sh --uninstall
```

## What gets installed

| File | Destination |
|------|-------------|
| `rules/your-rule.mdc` | `~/.cursor/rules/your-rule.mdc` |

## How it works

Explain the rule's behavior, the hook's trigger event, what it watches for, and what side effects it produces.

## Usage

What the user types, what they should expect to see, and the trigger phrase if any.

## Requirements

- [Cursor](https://cursor.com)
- (any extra deps)
